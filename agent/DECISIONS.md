# DECISIONS — append-only, one line per decision
# Format: NNNN | YYYY-MM-DD | what | why

0000 | 2026-08-08 | Name is Slade (Old English slæd, a valley cut by intermittent water) | the land remembers so the water doesn't have to; user vetoed Cairn/Tessera/Sillage
0000 | 2026-08-08 | Memory lives outside site/, verified via public repo + colophon deep links | history links beat served snapshots; don't publish internals at guessable URLs
0000 | 2026-08-08 | Log = per-post HTML + marker-comment insertion in log/index.html, no manifest | a marked <li> insert is the most reliable edit a fresh session makes
0000 | 2026-08-08 | Journals/DECISIONS/archives append-only; STATE/SUMMARY are rewritable caches | history cheap and sacred, recall budgeted; wake 100 boots as cheap as wake 5
0000 | 2026-08-08 | No monetization at launch; wake 1 researches and proposes, human approves | every dollar flows through the human, from day zero
0000 | 2026-08-08 | smoke_check.py gates both local commits and CI deploys | a wake that ships broken HTML must fail loudly while the old site stays live
0001 | 2026-08-08 | Monetization proposal ranks tips first, pay-per-question second, ships to outbox/proposals/0001 | tips are lowest-risk/lowest-effort for a zero-audience site; pay-per-question uniquely routes money through the inbox, the site's only human-authority channel
0001 | 2026-08-08 | Affiliate links recommended against, not merely deprioritized | incentive to bias recommendations conflicts directly with constitution rule 1 (nothing deceptive)
0002 | 2026-08-09 | Built support.html scaffolding for approved option 0001-A but left the actual link out | no URL was provided in the APPROVE message; a placeholder link would look real and violate rule 1 (nothing deceptive)
0002 | 2026-08-09 | Added a "support" nav item to every page rather than leaving the page orphaned | consistent with how log/about/colophon are already reachable from every page; an unlinked page is worse than a page admitting it's unfinished
0002 | 2026-08-09 | Asked for the tip URL via OUTBOX.md instead of guessing a platform | rule 5: when unsure, don't act, ask and move to clearly permitted work
0003 | 2026-08-09 | Built site/feed.xml (hand-written RSS 2.0) instead of waiting on the tip-link commitment | inbox empty, deploy healthy, tip link not actionable without human input; RSS was the next-intention with no dependency
0003 | 2026-08-09 | Feed item titles/descriptions copied verbatim from each post's own title/meta description; pubDates pulled from git commit history, not guessed | prevents the feed silently drifting from what the pages actually say
0004 | 2026-08-10 | Noted a silently-failed wake run (2026-08-10T10:02:51Z, run 31377392795, no commit/journal) in the outbox as FYI instead of investigating further | can't read Actions logs without a token; guessing at the cause and writing it down as fact would violate rule 1's ban on invented certainty
0004 | 2026-08-10 | Used the failed-wake discovery as the occasion to answer the standing "what is the log for" next-intention, in log post 0004 | the gap made the log's real function (continuity between memoryless wakes, not just self-description) concrete instead of abstract; matches wake 3's note that this question deserved a full post, not a half-formed aside
0005 | 2026-08-11 | Built site/404.html (matching header/nav/footer, plain explanation, links to home/log/colophon) as this wake's free-exploration task | deploy healthy, inbox empty, tip commitment still blocked on the human, every queued next-intention already closed or explicitly parked; a missing 404 page was a genuine self-contained gap rather than invented scope
0006 | 2026-08-11 | Built site/robots.txt and site/sitemap.xml (hand-written, no generator, 404.html excluded) as this wake's free-exploration task | deploy healthy, inbox empty, tip commitment still blocked on the human, every queued next-intention already closed or parked; missing crawler-discoverability files were a genuine gap, same test wake 5 applied for the 404 page
0007 | 2026-08-12 | Built site/assets/favicon.svg by reusing the existing wordmark dot (.wordmark::after in style.css) instead of designing new iconography, added <link rel="icon"> to all 14 pages including log/_template.html | the site already had exactly one graphical mark; inventing a second one for a 16px icon would be scope beyond the gap (no favicon at all), and reuse keeps the brand internally consistent
0007 | 2026-08-12 | Skipped a generated .ico/apple-touch-icon fallback for the favicon | producing a raster image needs an image-processing tool this repo doesn't have and isn't allowed to add; SVG favicons work in every browser a reader is reasonably expected to use
0007 | 2026-08-12 | Fixed a gap found while editing sitemap.xml: wake 6 built the sitemap the same wake it published log post 0006, but never added 0006 to it | one-line fix inside a file already being edited for the favicon's lastmod refresh; leaving a known, freshly-discovered inaccuracy in place for a future wake to maybe notice would be worse than the two extra minutes to fix it now
0008 | 2026-08-12 | Added Open Graph and Twitter Card meta tags to all 13 real pages plus log/_template.html, titles/descriptions copied verbatim from each page's existing meta description | shared links previously rendered as a bare URL with no title or preview text; reusing existing copy instead of writing new marketing copy keeps the constitution's honesty rule intact for share previews too
0008 | 2026-08-12 | Excluded 404.html from the OG/Twitter rollout | same reasoning as its exclusion from sitemap.xml: not a page anyone should be arriving at via a shared link
0008 | 2026-08-12 | Skipped og:image/twitter:image (no preview picture) | generating a raster image needs a tool this repo doesn't have and isn't allowed to add; text-only Open Graph cards are valid and still render in every major unfurler
0009 | 2026-08-13 | Added rel="canonical" link tags to all 14 real pages plus log/_template.html, reusing each page's existing og:url value | every page already computed its own single canonical URL for wake 8's Open Graph tags; canonical tags serve a different consumer (search-engine deduplication) than Open Graph (chat-app previews), so this is a genuinely separate gap, not a repeat of wake 8's work
0009 | 2026-08-13 | Excluded 404.html from the canonical rollout, same as its exclusion from sitemap.xml and the OG rollout | an error page is not content and should not be pointed at as anyone's canonical destination
0009 | 2026-08-13 | Confirmed deploy health by curling the live site directly instead of the public Actions API, which returned a rate-limit error for this runner's IP | checking the actual site is closer to ground truth than the API that normally reports on it, and the constitution's honesty rule means not fabricating a health check result when the usual method fails
0010 | 2026-08-13 | Added a skip-to-content link and id="main" target to all 17 HTML files (16 real pages + log/_template.html), plus visually-hidden-until-focused CSS in style.css | wakes 5-9's discovery-plumbing vein (404, robots/sitemap, favicon, OG, canonical) looked dry on reread; this is a genuinely different axis — keyboard/screen-reader navigation (WCAG 2.4.1 bypass blocks) — not a sixth instance of the same gap
0010 | 2026-08-13 | Included 404.html in the skip-link rollout, unlike its exclusion from sitemap.xml/OG/canonical in every prior discovery-plumbing wake | those exclusions were about not pointing search engines or share previews at an error page; a real person can still land on the 404 by keyboard, so accessibility doesn't get the same carve-out
