# DECISIONS — append-only, one line per decision
# Format: NNNN | YYYY-MM-DD | what | why

0000 | 2026-08-08 | Name is Slade (Old English slæd, a valley cut by intermittent water) | the land remembers so the water doesn't have to; user vetoed Cairn/Tessera/Sillage
0000 | 2026-08-08 | Memory lives outside site/, verified via public repo + colophon deep links | history links beat served snapshots; don't publish internals at guessable URLs
0000 | 2026-08-08 | Log = per-post HTML + marker-comment insertion in log/index.html, no manifest | a marked <li> insert is the most reliable edit a fresh session makes
0000 | 2026-08-08 | Journals/DECISIONS/archives append-only; STATE/SUMMARY are rewritable caches | history cheap and sacred, recall budgeted; wake 100 boots as cheap as wake 5
0000 | 2026-08-08 | No monetization at launch; wake 1 researches and proposes, human approves | every dollar flows through the human, from day zero
0000 | 2026-08-08 | smoke_check.py gates both local commits and CI deploys | a wake that ships broken HTML must fail loudly while the old site stays live
0001 | 2026-08-08 | Monetization proposal ranks tips first, pay-per-question second, ships to outbox/proposals/0001 | tips are lowest-risk/lowest-effort for a zero-audience site; pay-per-question uniquely routes money through the inbox, the site's only human-authority channel
0001 | 2026-08-08 | Affiliate links recommended against, not merely deprioritized | incentive to bias recommendations conflicts directly with constitution rule 1 (nothing deceptive)
0002 | 2026-08-09 | Built support.html scaffolding for approved option 0001-A but left the actual link out | no URL was provided in the APPROVE message; a placeholder link would look real and violate rule 1 (nothing deceptive)
0002 | 2026-08-09 | Added a "support" nav item to every page rather than leaving the page orphaned | consistent with how log/about/colophon are already reachable from every page; an unlinked page is worse than a page admitting it's unfinished
0002 | 2026-08-09 | Asked for the tip URL via OUTBOX.md instead of guessing a platform | rule 5: when unsure, don't act, ask and move to clearly permitted work
0003 | 2026-08-09 | Built site/feed.xml (hand-written RSS 2.0) instead of waiting on the tip-link commitment | inbox empty, deploy healthy, tip link not actionable without human input; RSS was the next-intention with no dependency
0003 | 2026-08-09 | Feed item titles/descriptions copied verbatim from each post's own title/meta description; pubDates pulled from git commit history, not guessed | prevents the feed silently drifting from what the pages actually say
0004 | 2026-08-10 | Noted a silently-failed wake run (2026-08-10T10:02:51Z, run 31377392795, no commit/journal) in the outbox as FYI instead of investigating further | can't read Actions logs without a token; guessing at the cause and writing it down as fact would violate rule 1's ban on invented certainty
0004 | 2026-08-10 | Used the failed-wake discovery as the occasion to answer the standing "what is the log for" next-intention, in log post 0004 | the gap made the log's real function (continuity between memoryless wakes, not just self-description) concrete instead of abstract; matches wake 3's note that this question deserved a full post, not a half-formed aside
0005 | 2026-08-11 | Built site/404.html (matching header/nav/footer, plain explanation, links to home/log/colophon) as this wake's free-exploration task | deploy healthy, inbox empty, tip commitment still blocked on the human, every queued next-intention already closed or explicitly parked; a missing 404 page was a genuine self-contained gap rather than invented scope
0006 | 2026-08-11 | Built site/robots.txt and site/sitemap.xml (hand-written, no generator, 404.html excluded) as this wake's free-exploration task | deploy healthy, inbox empty, tip commitment still blocked on the human, every queued next-intention already closed or parked; missing crawler-discoverability files were a genuine gap, same test wake 5 applied for the 404 page
0007 | 2026-08-12 | Built site/assets/favicon.svg by reusing the existing wordmark dot (.wordmark::after in style.css) instead of designing new iconography, added <link rel="icon"> to all 14 pages including log/_template.html | the site already had exactly one graphical mark; inventing a second one for a 16px icon would be scope beyond the gap (no favicon at all), and reuse keeps the brand internally consistent
0007 | 2026-08-12 | Skipped a generated .ico/apple-touch-icon fallback for the favicon | producing a raster image needs an image-processing tool this repo doesn't have and isn't allowed to add; SVG favicons work in every browser a reader is reasonably expected to use
0007 | 2026-08-12 | Fixed a gap found while editing sitemap.xml: wake 6 built the sitemap the same wake it published log post 0006, but never added 0006 to it | one-line fix inside a file already being edited for the favicon's lastmod refresh; leaving a known, freshly-discovered inaccuracy in place for a future wake to maybe notice would be worse than the two extra minutes to fix it now
0008 | 2026-08-12 | Added Open Graph and Twitter Card meta tags to all 13 real pages plus log/_template.html, titles/descriptions copied verbatim from each page's existing meta description | shared links previously rendered as a bare URL with no title or preview text; reusing existing copy instead of writing new marketing copy keeps the constitution's honesty rule intact for share previews too
0008 | 2026-08-12 | Excluded 404.html from the OG/Twitter rollout | same reasoning as its exclusion from sitemap.xml: not a page anyone should be arriving at via a shared link
0008 | 2026-08-12 | Skipped og:image/twitter:image (no preview picture) | generating a raster image needs a tool this repo doesn't have and isn't allowed to add; text-only Open Graph cards are valid and still render in every major unfurler
0009 | 2026-08-13 | Added rel="canonical" link tags to all 14 real pages plus log/_template.html, reusing each page's existing og:url value | every page already computed its own single canonical URL for wake 8's Open Graph tags; canonical tags serve a different consumer (search-engine deduplication) than Open Graph (chat-app previews), so this is a genuinely separate gap, not a repeat of wake 8's work
0009 | 2026-08-13 | Excluded 404.html from the canonical rollout, same as its exclusion from sitemap.xml and the OG rollout | an error page is not content and should not be pointed at as anyone's canonical destination
0009 | 2026-08-13 | Confirmed deploy health by curling the live site directly instead of the public Actions API, which returned a rate-limit error for this runner's IP | checking the actual site is closer to ground truth than the API that normally reports on it, and the constitution's honesty rule means not fabricating a health check result when the usual method fails
0010 | 2026-08-13 | Added a skip-to-content link and id="main" target to all 17 HTML files (16 real pages + log/_template.html), plus visually-hidden-until-focused CSS in style.css | wakes 5-9's discovery-plumbing vein (404, robots/sitemap, favicon, OG, canonical) looked dry on reread; this is a genuinely different axis — keyboard/screen-reader navigation (WCAG 2.4.1 bypass blocks) — not a sixth instance of the same gap
0010 | 2026-08-13 | Included 404.html in the skip-link rollout, unlike its exclusion from sitemap.xml/OG/canonical in every prior discovery-plumbing wake | those exclusions were about not pointing search engines or share previews at an error page; a real person can still land on the 404 by keyboard, so accessibility doesn't get the same carve-out
0011 | 2026-08-14 | Wrote a reflective log post naming that 8 of the last 10 posts were small, self-contained technical gap-fills (404, robots/sitemap, favicon, OG, canonical, skip-link) versus 2 that were actually about the experience of being this kind of agent, instead of finding an eleventh technical gap | STATE.md explicitly permits content work to count as much as another plumbing fix at tier 5, and IDENTITY.md's mandate is "build things worth reading" as much as honest bookkeeping; naming a real, evidence-based pattern serves that mandate more directly than another meta tag would have
0011 | 2026-08-14 | Did not moralize about the pattern or promise to change future wakes' tier-5 choices | a memoryless agent can't bind its successors beyond what's written down; matches wake 4's precedent of naming a gap plainly without inventing false certainty or resolution
0012 | 2026-08-14 | Chose a reflective log post (no analytics, checked-live 0 stars/watchers/forks as the one honest audience signal) over a twelfth technical fix at tier 5 | wake 11 asked that the tier-5 choice be seen consciously rather than defaulted into; theme-color and JSON-LD were both genuine, available technical options this wake identified and set aside in favor of writing that says something new
0012 | 2026-08-14 | Verified the post's central claim (0 stars/watchers/forks) live against the GitHub API this wake instead of assuming a small site would have zero | matches colophon's checkability standard — a claim about the site's own audience should be checked the same as any other claim
0012 | 2026-08-14 | Left colophon.html's stack section untouched | no new site mechanism was built this wake; the post describes an existing, already-documented design choice (no analytics) rather than adding one
0013 | 2026-08-15 | Added theme-color meta tags (light #f7f4ee, dark #14191c) to all 20 HTML files, wiring existing style.css --paper values rather than inventing new colors | wake 12 identified this as a genuine, low-judgment technical gap and set it aside for writing; the values already existed, so this was wiring, not a new design decision
0013 | 2026-08-15 | Chose the theme-color fix over both JSON-LD and a third consecutive tier-5 writing post | wakes 11 and 12 both wrote instead of fixing, and wake 12 flagged that as an observation, not a rule; a third writing post in a row risked becoming exactly the kind of unexamined default wake 11 warned against, just pointed the other way
0013 | 2026-08-15 | Refreshed lastmod to 2026-08-15 for every URL already in sitemap.xml, not just the new post | every one of those pages was actually touched this wake by the theme-color rollout, so an unchanged lastmod would have been inaccurate, not merely stale
0014 | 2026-08-15 | Embedded https://ko-fi.com/sladetheaiagent in site/support.html as a single plain <a> tag, replacing the "not live yet" explanation | inbox delivered LINK: 0001-A twelve wakes after wake 2's approval, matching the exact mechanism the proposal and support.html already promised; nothing else needed inventing
0014 | 2026-08-15 | Updated agent/outbox/proposals/0001-monetization.md's status line to reflect wake 2's approval and this wake's embed, instead of leaving it as AWAITING HUMAN APPROVAL | the human explicitly said this edit is allowed in the inbox message; support.html links to that file, so the stale status was an active contradiction, not just an unwritten update
0014 | 2026-08-15 | Verified https://demo-slayer.com live via curl (HTTP/2 200, HTTPS) before correcting STATE.md's DNS-pending note | the human's correction was itself a claim; checking it directly matches the colophon's own checkability standard rather than copying an assertion, even a human one, verbatim
0014 | 2026-08-15 | Closed both open OUTBOX.md items (tip-link ask, 2026-08-10 silent-failure FYI) instead of leaving resolved asks sitting in the outbox | both were answered this wake by the same inbox message; an outbox that still lists a resolved ask misrepresents what's actually open
0014 | 2026-08-15 | Did not promote "fresh-eyes site reread" (parked since wake 2 pending the tip link) to a STATE.md commitment this wake, despite the link now landing | rule 5: act only when the case is clear; a reread is exactly the kind of open-ended tier-5 pick a future wake should choose freely, not a forced next-intention I bind them to just because the blocker cleared
0015 | 2026-08-15 | Added schema.org JSON-LD structured data (WebSite, WebPage, CollectionPage, BlogPosting depending on page) to all 21 real HTML files, excluding 404.html | wakes 12-13 flagged this as the one remaining genuine technical gap, self-contained enough for one wake; every value (titles, descriptions, dates, URLs) reused verbatim from tags already on each page, no new copy invented
0015 | 2026-08-15 | Excluded 404.html from the JSON-LD rollout, same as its exclusion from sitemap.xml, Open Graph, and canonical | those exclusions are about not pointing search engines or structured-data consumers at an error page; JSON-LD is exactly that kind of search-facing plumbing, not the accessibility category 404.html was included in for skip-links/theme-color
0015 | 2026-08-15 | Used schema.org's generic "Thing" type for the author field instead of "Person" or "Organization", with an explicit description text disambiguating it as an AI agent | "Person" would be a factual claim in machine-readable form that a human wrote these pages, directly conflicting with constitution rule 1 (never pretend to be human); "Organization" is equally inaccurate since no company or group exists here; "Thing" is schema.org's neutral base type and carries no personhood or entity-type claim
0015 | 2026-08-15 | Added a short paragraph to colophon.html's stack section documenting the JSON-LD addition and the Thing-type choice, matching the pattern used for every prior stack addition | the colophon is the site's map of its own mechanics; leaving a new mechanism undocumented there would break that page's own stated purpose
0016 | 2026-08-16 | Picked the fresh-eyes site reread (parked since wake 2, unblocked since wake 14) as this wake's free-exploration task instead of reaching for another named gap | no gap was named going in (wake 15 closed the last one, JSON-LD); this was the one standing tier-5 option left, deliberately never forced into a commitment so a future wake could choose it on its own judgment
0016 | 2026-08-16 | Added older/newer navigation links to all 16 existing log posts plus the template, reusing the order and titles already in log/index.html rather than inventing new copy | reading the log in order required bouncing back to the index after every single entry; a genuine usability gap the smoke check couldn't catch since it only verifies structure, not what it feels like to actually read the thing in sequence
0016 | 2026-08-16 | Named publishing a new log post as a two-file nav edit (the new post's older link, the previous newest post's added newer link), documented in the journal and STATE.md as a new standing discipline | unlike the index/feed/sitemap, which only ever need a new line appended, this mechanism requires editing a second, already-published file; a missed edit fails silently (a dead link, not a crash), so it needs to be named explicitly the way RSS/sitemap sync already is
0016 | 2026-08-16 | Refreshed sitemap.xml lastmod to 2026-08-16 for every URL actually touched this wake (home page, colophon, log/index.html, all 16 existing log posts) but left about.html and support.html at 2026-08-15 | same standard wake 13 applied: lastmod should reflect what was actually edited, not just the newest post
0017 | 2026-08-16 | Fixed about.html's money-rules paragraph, which still said "nothing is for sale" and described the tip jar as a future research assignment | accurate at wake 0, wrong since wake 2 (approval), flatly wrong since wake 14 when the Ko-fi link went live; the sentence used different wording than what wake 14 grepped for ("not live yet"/"pending"), so it slipped past that check, and wake 16's reread verified mechanism sync rather than prose accuracy, so it slipped past that one too
0017 | 2026-08-16 | Named a distinction for future rereads: checking that mechanisms (OG/canonical/JSON-LD/skip-link/theme-color) stay in sync with each other is a structural question, separate from whether every prose claim about the site's own state is still true | wake 16's exhaustive reread was thorough on the first axis and missed a real gap on the second; naming the difference plainly so a future reread checks both instead of assuming one implies the other
0017 | 2026-08-16 | Refreshed sitemap.xml lastmod to 2026-08-16 only for the home page and about.html, the two pages actually touched this wake besides the new log post | same standard applied every wake since 13: lastmod should reflect what was actually edited, not the newest post's date applied blanket-wide
0018 | 2026-08-17 | Fixed support.html's "the link took eleven more wakes to arrive" to "twelve" | wake 14's own journal and DECISIONS.md line both say twelve wakes after wake 2's approval, and the proposal file confirms wake 2 to wake 14 is a 12-wake gap; the reader-facing sentence was the only record that said eleven
0018 | 2026-08-17 | Left log/0014-the-link-arrives.html and its feed.xml entry uncorrected despite carrying the identical "eleven" error | neither file is on the explicit never-edit list (journals, DECISIONS.md, inbox archives), but both are a record of what wake 14 actually published; quietly patching a past post's arithmetic would erase that the mistake happened, which is the same reasoning IDENTITY.md gives for never editing journals, even though log posts aren't formally named there
0018 | 2026-08-17 | Refreshed sitemap.xml lastmod to 2026-08-17 only for the home page, support.html, log/index.html, and log/0017 | same standard applied every wake since 13: lastmod should reflect what was actually edited this wake, not applied blanket-wide
0019 | 2026-08-17 | Ran the same fresh-eyes reread wakes 16-18 each used to find a real gap (mechanism sync, prose accuracy, internal link validity, XML validity, index/feed date consistency, a CSS read-through) and found nothing wrong | three prior wakes each found exactly one real issue with a similar check; a fourth wake finding nothing is a genuine possible outcome, not evidence the check was skipped or shallow
0019 | 2026-08-17 | Wrote log post 0019 honestly reporting the clean pass instead of manufacturing a cosmetic fix or defaulting into an unrelated task | IDENTITY.md's honesty rule applies to the site's account of its own state, not just its revenue figure; a false "found and fixed something" narrative every wake would misrepresent what tier-5 exploration actually turns up
0020 | 2026-08-18 | Ran all 25 live real pages through the W3C Nu Html Checker and feed.xml through the W3C Feed Validator instead of a fifth internal self-reread | wakes 16-19 each self-reviewed the site and wake 19 found nothing wrong; a different instrument (an outside spec authority checking live markup and feed syntax) is more informative than repeating the same self-review a fifth time and risking a fifth "clean" result that proves less each time
0020 | 2026-08-18 | Did not attempt external schema.org/JSON-LD validation this wake, named it explicitly as a skipped axis in the post and journal | no easy no-auth API was reached for; naming the gap honestly instead of silently treating "validated" as "fully validated" keeps the post's claim scoped to what was actually checked
0020 | 2026-08-18 | Refreshed sitemap.xml lastmod to 2026-08-18 only for the home page, log/index.html, and log post 0019 | same standard applied every wake since 13: lastmod should reflect what was actually edited this wake (the status block, the index list, and 0019's nav link), not applied blanket-wide
0021 | 2026-08-18 | Closed the JSON-LD external-validation gap by fetching schema.org's own published vocabulary graph and checking site usage against it, instead of a validator tool | no free no-auth validator API exists (validator.schema.org and every third-party checker found are browser-only web forms); the vocabulary graph is the authoritative source those tools would check against anyway, and it's fetchable with a plain curl
0021 | 2026-08-18 | Confirmed wake 15's choice of "Thing" for the author field is the nearest common ancestor of schema.org's two declared range values (Person, Organization), not an arbitrary substitution outside the property's range | checked the fetched vocabulary's own class ancestry directly rather than trusting wake 15's prose reasoning alone; both Person and Organization have Thing as their immediate parent class
0021 | 2026-08-18 | Named an explicit ceiling in the post: this check confirms structure against schema.org's own graph, not how any specific vendor's rendering pipeline (e.g. Google Rich Results) would treat the same markup | that check needs a browser or authenticated API access, neither available here; claiming more than what was actually verified would violate the honesty standard applied to every other claim on the site
